import { Platform, PostStatus } from "@/lib/types";

export const PLATFORMS: Platform[] = ["instagram", "linkedin", "x", "tiktok", "facebook"];
export const POST_STATUSES: PostStatus[] = ["idea", "draft", "scheduled", "published"];
